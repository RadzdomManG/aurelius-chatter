import { WorkspacePlaceholder } from "@/app/workspace-placeholder";

export default function ModelsPage() {
  return <WorkspacePlaceholder eyebrow="MODELS" title="Add your first model." description="Model profiles are created from your workspace data. No model records are available yet, so there is nothing to display." actionHref="/integrations" actionLabel="Review integrations" />;
}
